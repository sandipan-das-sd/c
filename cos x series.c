#include<stdio.h>
#include<math.h>
series(float);
{
  temp=1;
 for(i=0;i<=n;i+2)
 {
 	term=neu/deno;
 	neu=pow(x,i+2);
 	deno=
 }
int main()
{
	int degree,temp;
	int term;
	int neu,sum=0;
	int deno;
	printf("Enter the value of degree:-");
	scanf("%d",&degree);
	series(float);
}
